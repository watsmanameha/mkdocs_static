{
  "name": "mkdocs_static",
  "private": true,
  "scripts": {
    "site:build": "mkdocs build --strict --site-dir site",
    "css:build": "postcss src/styles.css -o site/assets/styles.css",
    "js:build": "esbuild src/app.js --bundle --minify --outfile=site/assets/app.js",
    "html:validate": "npx htmlhint 'site/**/*.html'",
    "html:minify": "npx html-minifier-terser --input-dir site --output-dir site --collapse-whitespace --remove-comments --minify-css true --minify-js true --file-ext html",
    "w3c:validate": "docker run --rm -v \"$PWD/site:/site\" ghcr.io/validator/validator:latest /vnu-runtime-image/bin/vnu --skip-non-html --errors-only /site",
  "typograf": "npx typograf --type name --locale ru,en-US --html true --only-changed --recursive site",
  "typograf:cli": "node scripts/typograf.js",
  "build": "npm run site:build && npm run css:build && npm run js:build && npm run html:validate && npm run w3c:validate && npm run html:minify",
  "build:typograf": "npm run build && npm run typograf:cli"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "cssnano": "^6.0.1",
    "esbuild": "^0.21.5",
    "htmlhint": "^1.1.4",
    "html-minifier-terser": "^7.2.0",
    "postcss": "^8.4.39",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.4",
    "typograf": "^7.4.1"
  }
}
